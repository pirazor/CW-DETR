# ============================================================================
# CW-DETR — ConnectedWise multi-task ADAS perception model
# Core training/research dependencies (x86 workstation w/ NVIDIA GPU).
# For Jetson Orin deployment deps (TensorRT, torch2trt) see export/README.
# ============================================================================

# --- Deep learning core ---
torch>=2.3.0
torchvision>=0.18.0

# --- Foundation backbone (DINOv3) ---
transformers>=4.56.0      # DINOv3 model classes (facebook/dinov3-*)
timm>=1.0.11              # convnext_*.dinov3_lvd1689m weights, layers
einops>=0.8.0
safetensors>=0.4.3

# --- Detection / DETR utilities ---
supervision>=0.24.0      # sv.Detections output format (RF-DETR compatible)
pycocotools>=2.0.7
scipy>=1.11.0            # linear_sum_assignment (Hungarian matcher)

# --- Tracking ---
lap>=0.4.0               # ByteTrack association (Jonker-Volgenant)
cython-bbox>=0.1.3
motmetrics>=1.4.0        # MOTA/IDF1 evaluation

# --- Datasets ---
nuscenes-devkit>=1.2.0   # nuScenes tracking + prediction; Python 3.12 support
# bdd100k labels parsed directly from JSON; mapillary/gtsrb via custom loaders

# --- Image / data ---
numpy>=1.24.0
opencv-python>=4.9.0
Pillow>=10.2.0
pyyaml>=6.0.1

# --- Export / deployment ---
onnx>=1.16.0
onnxruntime-gpu>=1.18.0
onnxslim>=0.1.34         # graph simplification before TensorRT

# --- Logging / dev ---
tensorboard>=2.16.0
tqdm>=4.66.0
pytest>=8.0.0
